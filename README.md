# flappy-bird

flappy-bird game written by `react` and `create-react-app`

1. Go into your terminal, and run `npm i` - wait for the necessary packages to install
2. In your terminal, run `npm run build` - again, wait for build files to generate
3. In your terminal, run `npm run start` to initiate the game.
